// 第一個值對應每個資料夾的 tu.png（種子圖），已調整為 40px
export const DEFAULT_PLANT_IMAGE_SIZES = [35, 35, 50, 65, 85, 110];

// 對應每個資產的垂直偏移 (px)。第一個值對應 tu.png，預設往下移 100px
export const DEFAULT_PLANT_IMAGE_OFFSETS = [115, 80, 65, 60, 35, 10];

// 對應每個資產的水平偏移 (px)。第一個值對應 tu.png，負值往左移 (預設往左 4px)
export const DEFAULT_PLANT_IMAGE_XOFFSETS = [-4, -2, -4, -2, -2, -2];
